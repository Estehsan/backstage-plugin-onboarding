{
  "name": "onboarding-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "plugins/*"
    ]
  },
  "scripts": {
    "tsc": "tsc",
    "build:all": "tsc && yarn workspace @estehsaan/backstage-plugin-onboarding-common build && yarn workspace @estehsaan/backstage-plugin-onboarding build && yarn workspace @estehsaan/backstage-plugin-onboarding-backend build",
    "clean": "yarn workspaces foreach --all --parallel run clean"
  },
  "devDependencies": {
    "typescript": "~5.4.0"
  }
}
